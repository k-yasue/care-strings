# 引き継ぎメモ — CareStrings(ケアストリングス)プロジェクト

- 版: v3 / 更新日: 2026-07-16
- 用途: 新しいAIチャットスレッドや後日の自分への引き継ぎ。このファイルを冒頭に貼れば文脈を復元できる。

---

## 1. プロジェクト概要

- アプリ名: **CareStrings**。由来=入居者や家族の思いが「弦(strings)」となって職員に共有され繋がるイメージ。README・要件定義書§1.4に記載済み。
- 特養(特別養護老人ホーム)向けの申し送り・情報共有Webアプリ。**転職用ポートフォリオ**として個人開発する。
- 志望企業: **ALTURA X株式会社**(医療SaaSスタートアップ、健診DX×PHR、HL7 FHIR活用)。利用技術スタック(確認済み): **Go / TypeScript / React / React Native / AWS / MySQL / Redis / GraphQL / gRPC**。求人業務内容に「GoによるAPI開発(標準ライブラリ中心、GraphQLスキーマ設計含む)」、歓迎条件に「GoによるGraphQL・gRPC実装経験」。ジュニア枠には「Go未経験歓迎」あり。
- 開発者スキル: VB.NET 1年、C# 約1.5年、HTML/CSS/JS 3〜4年。**Goは未経験**。AIをペアプロ相手に学びながら作る方針(隠さず「AI活用+Go初挑戦」とREADMEに明記する戦略で合意済み)。

## 2. 原体験と課題(このプロジェクトの核)

- 開発者の**母が要介護5で特養に入居中**。母の不満:「職員に伝えたことが他の職員に共有されていない。何度も同じことを言わされる」= ケアを受ける側から見た情報共有の失敗。
- 特に外国人職員への伝達が不確実。身近に「**日本語の会話はできるが漢字が読めない**」外国人職員が実在する。
- 業界調査でも裏付けあり: 情報共有の課題1位「申し送りノートの不明点確認」、2位「引き継ぎ」。原因「内容を確認しない人がいる」「解釈の違い」「手書きで履歴が残らず言った言わないになる」(ケアきょうのアンケート)。
- 施設の職員に**ヒアリング可能**(母の入居先の職員と話せる)。

## 3. 確定した技術構成

| 領域         | 技術                                                             |
| ------------ | ---------------------------------------------------------------- |
| フロント     | Next.js (App Router) + TypeScript                                |
| バックエンド | Go(標準net/http中心)+ gqlgen による GraphQL API ※フェーズ2で移行 |
| DB/認証/画像 | Supabase (PostgreSQL + Auth + Storage)                           |
| ふりがな     | kuroshiro(クライアント側生成、DB保存しない)                      |
| 翻訳         | DeepL API等(投稿時に翻訳→DBキャッシュ方式)                       |
| AI要約       | Claude API                                                       |
| UI多言語     | next-intl(i18n辞書)                                              |
| ホスティング | Vercel(フロント)+ Cloud Run/Railway(Go)                          |
| PWA          | 後から追加(最後でよい)                                           |

- 開発順序: **フェーズ1= Next.js+Supabase直結でMVP完成・公開 → フェーズ2= Go(標準net/http中心)でAPI層を構築。最初の2〜3エンドポイントはRESTで肩慣らし後、gqlgenでGraphQL APIとして本実装**(志望先のスタックにGraphQL明記のため計画に昇格。投稿詳細=投稿+著者+翻訳+確認状況+コメントの1クエリ取得がスキーマ設計の見せ場)。コミット履歴に過程を残す。
- AWSは使わない(無料枠と開発速度優先。面接では「小規模個人開発での費用対効果判断」と説明)。gRPCは見送り(この規模で使うと不自然。ロードマップに「サービス分割時に検討」と記載する程度)。

## 4. 主要な設計判断(合意済み)

1. **既読と確認を分離**: 既読=詳細を開いたら自動記録(read_at)/確認=「内容を確認しました」ボタン(confirmed_at)。post_receiptsテーブル(投稿×職員で1行)。重要投稿は確認必須で、ホーム上部バナーに確認まで滞留。
2. **表示モード3択**: 日本語 / **ふりがな付き日本語(本命。漢字が読めない職員向け)** / 翻訳(英・タイ語)。UIメニューも投稿内容も同じモード設定で切替。実装順: 投稿ふりがな→UI辞書→翻訳API。
3. **翻訳は投稿時キャッシュ方式**(表示のたびにAPIを叩かない)。UIメニューはi18n辞書(コストゼロ)。ふりがなはkuroshiroでクライアント生成(DBに持たない)。
4. **バイタルは「異常値→申し送り投稿を提案」で共有と接続**するのが目玉。基準値はハードコードせず施設設定(vital_thresholds)。
5. **フロー/ストックの分離**: 申し送り(posts=流れる)と恒常的な注意事項(resident_notes=利用者詳細に常時表示、カテゴリ別)を分けた。
6. **コメント機能あり(フラット・ネストなし)**: 現場課題1位「申し送りの不明点確認」への解答。投稿・コメントとも**編集不可**(「言った言わない」防止。訂正は新規投稿・コメントで)。
7. **通知2ルール**: ①自分の投稿にコメント ②自分が参加したコメント欄に新着 → 質疑ループが閉じる。ベルバッジ+通知一覧。
8. **管理者専用の登録**: 職員(招待メール)・利用者の登録/編集はadminのみ。UI出し分け+Supabase RLSの2層防御。
9. **棟タブ+絞り込みフィルタ**: ホームは棟(1棟/2棟/全体)タブ×(すべて/利用者宛/施設連絡)フィルタ。施設連絡=resident_id NULL に後から辿り着ける。
10. **投稿カードに投稿者のアイコン+名前+役職チップ**(所属棟は非表示=タブで自明)。ホームとダッシュボードは1画面統合(重要バナー+AI要約+タイムライン)。
11. 削除でなくis_activeフラグ(履歴保全)。全テーブルにupdated_at(トリガー自動更新)。命名規約=小文字スネークケース・複数形(一般的と確認済み)。

## 5. 法務・データ方針(重要、合意済み)

- **実在の入居者・職員の情報は一切入れない**。介護・健康情報は要配慮個人情報。名前だけ変えた仮名化でも照合可能なら個人情報のままなのでNG。
- 検証は**完全架空の利用者・シナリオ**を作り、職員に操作してもらい感想を聞く**ユーザビリティテスト**形式で実施。
- READMEに「実運用には医療情報ガイドライン準拠が必要。本アプリはデモ用途」と明記する。

## 6. 競合と差別化

- 既存: ケアコラボ(利用者軸タイムライン、申し送り時間短縮の実績)、ほのぼの、カイポケ等多数。
- 差別化: **ふりがな/多言語ネイティブ対応 + AI要約 + 既読/確認の分離**。ケアコラボは調べた範囲で多言語・AI要約を製品内蔵していない。
- ビジネスとして成功は困難(レッドオーシャン、施設のIT予算薄)だが、ポートフォリオ・転職の道具としては有望、という評価で合意。

## 7. 成果物とリポジトリ構成

GitHubリポジトリ **care-strings** 作成済み。設計書は docs/ に版番号なしで格納(版管理はGitコミット)。

```
care-strings/
├── CLAUDE.md                  # Claude Code用の作業標準(実装ルール記載)
├── docs/
│   ├── requirements.md        # 要件定義書(F-01〜17=MVP、F-18〜21=第2リリース)
│   ├── db_design.md           # テーブル設計書(12テーブル、設計判断10個)
│   ├── wireframe.html         # ワイヤーフレーム9画面
│   └── handoff.md             # 本ファイル
├── supabase/migrations/
│   └── 001_initial_schema.sql # 適用済み(12テーブル+トリガー+インデックス+RLS+シード)
└── (Next.jsソース)
```

## 8. 現在地と次のステップ

- 完了: 設計フェーズ全部 / Node.js・Next.js(App Router, TS)・GitHub環境構築 / **SupabaseプロジェクトにDBスキーマ適用済み(12テーブル稼働中、初期データ投入済み)** / CLAUDE.md整備 / ブランチ運用開始(認証以降はPR方式と合意)
- **現在: feature/auth ブランチで認証(F-01)実装に着手するところ**。手順: @supabase/supabase-js と @supabase/ssr 導入 → .env.local(URL+anonキー、gitignore確認)→ Supabaseクライアント(utils/supabase/)→ /loginページ → proxy.ts(未ログインは/loginへ)→ ログアウト。**サインアップ画面は作らない**(管理者招待方式)。テストユーザーはダッシュボードのAuthentication→Add user+SQLでprofilesにadmin行を手動作成
- 実装順: 認証(F-01)→ 職員/利用者管理・管理画面(F-02,03,17)→ 投稿・タイムライン(F-04〜07)→ 既読/確認(F-08,09)→ コメント・通知(F-14,15)→ 注意事項(F-16)→ ふりがな(F-11)→ バイタル(F-12,13)→ 公開 → Go+GraphQL移行 → 翻訳/i18n/AI要約/PWA(F-18〜21)
- 未着手の宿題: 職員ヒアリング(質問リストは docs/requirements.md §9)。「ふりがなと母語翻訳どちらが有用か」「必要な言語」「バイタルの測定頻度」を確認したい。

## 9. 未決定・保留事項

- 翻訳APIの最終選定(DeepLはタイ語対応可否を確認要。ベトナム語等が必要ならGoogle翻訳API寄り。ヒアリングで必要言語を確定してから選定)
- カテゴリの最終ラインナップ(現案: 食事/口腔ケア/排泄/睡眠/健康/家族連絡/その他 — ヒアリングで補正予定)
- 確認画面・設定画面・登録フォーム・マイページのワイヤーフレーム(定型画面のため省略、実装時に簡易作成)
- 将来拡張(ロードマップ): コメント引用(reply_to_comment_id)、@メンション(投稿は全員公開のまま指名者に通知。DMは作らない方針)、施設連絡のピン留め掲示板、多施設対応(facility_id+対応言語・カテゴリ・基準値の施設設定化)、FHIR、音声入力、家族向け閲覧ビュー、サービス分割時のgRPC検討

## 9.5 マイページ仕様(合意済み・ワイヤー省略)

②ホーム右上👤 → マイページ。

- 本人が変更可: アイコン画像 / 表示モード(ja・furigana・en・th、ヒアリング結果で追加) / パスワード / メールアドレス(Supabase標準の新アドレス確認メールフローに乗る)
- 本人は変更不可(adminのみ): 名前・役職・所属棟 — 投稿記録の同一性と権限昇格防止のため
- その他: 自分の投稿一覧・自分の未確認への導線、ログアウト
- 言語追加はCHECK制約+UI辞書+翻訳対象リストの追加のみ。過去投稿は遅延キャッシュで吸収

## 10. AIとの協働スタイル(引き継ぎ先AIへ)

- ユーザーは率直なフィードバック(忖度なし)を好む。過剰な専門用語は避け、都度かみ砕いて説明する(例: PWA、i18n、インデックス等は説明済み)。
- 提案は理由とトレードオフ付きで。ユーザーは自分で判断して取捨選択するスタイル(バイタル残す判断、実データNGの受け入れ等)。
- 成果物はダウンロード可能なファイル(md/html)で残す方針。
- 分業: このチャット(設計・レビュー・学習の相談役)+ Claude Code(実装係、CLAUDE.mdを参照)。

## 11. 実装時の仕様メモ(小さい決定事項の置き場)

- 下部ナビの「未確認」タブ → **「確認」タブ**に変更。タブ内で「未確認/確認済み」を切り替えて両方閲覧(自分の確認履歴の振り返り用。DB変更不要、post_receipts.confirmed_at の有無で絞る)
- 重要バナーは「**自分が confirmed_at を付けていない**重要投稿」のみ表示(人ごとに表示が異なり、確認すれば自分の画面から消える)
- 翻訳生成は**在籍職員の display_mode に存在する言語のみ**(APIコスト削減)
- カテゴリ追加候補: **medication(薬)** — ヒアリング後に判断。カテゴリ変更はマイグレーションでCHECK制約を差し替え。posts と resident_notes のカテゴリは将来分岐可(posts側にのみ announcement/instruction 等を追加できる)
- 投稿の宛先は「利用者」「棟/全体」の2軸のみ。**職員個人宛て(DM)は思想に反するため作らない**(オープン共有が原則。個人に届けたい場合は将来の@メンションで対応)
- RLSの notifications / post_translations のINSERTはフェーズ1では「ログイン職員なら可」に緩和中(サーバー不在のため)。**フェーズ2のGo移行時にサーバー専用へ締める**(段階的セキュリティ強化として語れる)
- **Next.js 16 で `middleware` は `proxy` に改名された**(機能は同じ)。ファイルはプロジェクト直下の `proxy.ts`、関数名も `proxy`。世の中のSupabase認証チュートリアルは軒並み `middleware.ts` 表記なので**そのまま真似すると動かない**。根拠はバンドル版ドキュメント `node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md`
- 公式は「proxyを認証・セッション管理の本体にするな」と明記。**proxyは門前払い(optimistic check)と セッション延命に限定し、本当の防御はRLSとページ側で行う**(既存の「UI出し分け+RLS 2層防御」方針と一致)
- Supabaseのキーは新方式。**Publishable key(`sb_publishable_...`)を使用**。旧anonキーに相当し公開前提。環境変数名は `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`。Secret key(`sb_secret_...`)はRLSを無視するのでフェーズ1では絶対に使わない
